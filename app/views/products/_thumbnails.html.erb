<% if product.images.size > 1 %>
  <% product.images.each do |i| %>
    <%= link_to_remote image_tag(i.attachment.url(:mini)), :url => change_image_product_path(product, :image_id => i) , :update => "product_image" %>
  <% end %>
<% end %>
